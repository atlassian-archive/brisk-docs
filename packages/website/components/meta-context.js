import * as React from 'react';

const ContextMeta = React.createContext({});

export default ContextMeta;

/* eslint-disable-next-line import/no-unresolved */
export { metadata } from '../data/site-meta.json';
